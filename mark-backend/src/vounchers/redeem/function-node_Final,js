/**
 * Build Redeem Response
 *
 * Objetivo:
 *   - Retornar para o aluno as instruções do voucher resgatado.
 *   - Isso é o que o app mostra na tela final tipo “Parabéns, resgate concluído”.
 *
 * Contrato de resposta sugerido:
 * {
 *   "message": "Voucher resgatado com sucesso!",
 *   "voucherCode": "AMZ-GIFT-1234-5678",
 *   "instructions": "Use este código no site do parceiro até 30 dias."
 * }
 *
 * Segurança:
 *   - Não retornamos saldo, transações internas etc.
 *   - Esse endpoint não precisa confirmar o novo saldo aqui.
 */

return [
  {
    json: {
      message: 'Voucher resgatado com sucesso!',
      voucherCode: $json.voucherCode,
      instructions: $json.instructions,
      statusCode: 200,
    },
  },
];
